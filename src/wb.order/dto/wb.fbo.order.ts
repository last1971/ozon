export class WbFboOrder {
    gNumber: string;
    date: string;
    incomeID: number; // Номер поставки (от продавца на склад)
    isCancel: boolean;
    srid: string;
    supplierArticle: string;
    totalPrice: number;
}
