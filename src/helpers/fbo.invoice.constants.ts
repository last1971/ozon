/**
 * Признак FBO-счёта в `S.IGK`. Ставится при создании счёта FBO-веткой и служит
 * ЕДИНСТВЕННЫМ надёжным дискриминатором схемы в таблице `S`: покупатель у Ozon FBO
 * и Ozon FBS один и тот же (`OZON_BUYER_ID`), номера отправлений одного формата.
 *
 * Правило владельца: `NOT1C` — атрибут FBO-счёта и с созданием он уже стоит,
 * дописывать его на других путях нельзя (см. order.service.ts, ветки отмен).
 */
export const FBO_INVOICE_IGK = 'NOT1C';
