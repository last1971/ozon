{{#each orders}}
    <div>
        <span>Номер заказа: {{ prim }}</span>
        <span>Артикул: {{ offer_id }}</span>
    </div>
{{/each}}
<p style="color: #999; font-size: 11px; margin-top: 20px;">Server: {{ serverName }}</p>